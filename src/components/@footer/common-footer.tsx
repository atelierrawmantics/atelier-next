'use client'

import { Button } from '@/components/ui/button'

export const CommonFooter = () => {
  return (
    <footer className="w-full py-[80px] px-[40px] gap-[48px] flex flex-col items-center justify-center bg-primary-4"></footer>
  )
}
